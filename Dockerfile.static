# Dockerfile ultra-simples: build local e deploy estático
FROM nginx:alpine

# Copiar arquivos web pré-construídos (você deve fazer flutter build web antes)
COPY build/web /usr/share/nginx/html

# Copiar configuração nginx
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
